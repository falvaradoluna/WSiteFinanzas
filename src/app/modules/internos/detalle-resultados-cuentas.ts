export interface IDetalleResultadosCuentas {
  MOV_TIPOPOL: string,
  MOV_CONSPOL: number,
  MOV_CONSMOV: number,
  MOV_MES: number,
  MOV_NUMCTA: string,
  MOV_CONCEPTO: string,
  MOV_DEBE: number,
  MOV_HABER: number
}
